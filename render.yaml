services:
  - type: web
    name: indexviz
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /api/students/stats
    envVars:
      - key: JAVA_TOOL_OPTIONS
        value: "-Xmx256m"
